use bevy::prelude::*;

/// Main game plugin (placeholder for now)
pub struct GamePlugin;

impl Plugin for GamePlugin {
    fn build(&self, _app: &mut App) {
        // Game logic will be implemented here later
    }
}
